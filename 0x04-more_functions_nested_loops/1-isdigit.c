#include "main.h"
#include <stdio.h>

/**
 * _isdigit - a function that checks for a digit
 * @c: a parameter
 * Return: 1 if digit or 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
