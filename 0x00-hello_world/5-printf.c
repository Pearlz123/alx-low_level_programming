#include <stdio.h>
#include <string.h>
/**
 * main-prints an expression as it is
 * Return: 0 if exited properly and non zero if otherwise
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art\n");
return (0);
}
