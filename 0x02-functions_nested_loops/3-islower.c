#include <stdio.h>
#include <ctype.h>
/**
 * _islower(int c) - a function that checks for lowercase character
 * Return: Returns 1 if c is lowercase or 0 otherwise
 */
int _islower(int c)
{
int r;
for (r = 'a'; r <= 'z'; r++)
{
printf('const' char *__restrict __format, 1, islower(r));
}
return (0);
}
