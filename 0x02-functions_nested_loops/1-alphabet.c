#include "main.h"
/**
 * print_alphabet - Write a program that prints the alphabet in lowercase
 * Return: 0 something
 */
void print_alphabet(void)
{
int ch;

for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
}
_putchar('\n');
}
